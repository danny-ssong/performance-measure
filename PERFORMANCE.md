# 주문 그리드 렌더 성능 측정

## playwright

- 반드시 headed, full-screen size로 테스트한다.

## 0. 왜 만들었나

**목표:** 조회 버튼을 눌렀을 때 테이블 렌더가 얼마나 걸리는지 측정하고, 가상화와 트랜지션을 추가했을 때 성능 개선을 측정한다.

두 기법은 성격이 다릅니다. 이 차이가 이 문서 전체의 뼈대입니다.

| 기법     | 하는 일                                          | 줄어드는 것                        | 볼 지표                                              |
| -------- | ------------------------------------------------ | ---------------------------------- | ---------------------------------------------------- |
| 가상화   | 그릴 노드 수 자체를 줄인다                       | **총 작업량**                      | `결과 표시 시간` + 작업 분해(스크립트·렌더링·페인트) |
| 트랜지션 | 총량은 그대로 두고 **잘게 쪼개** 입력에 양보한다 | 총량은 그대로, **가장 긴 한 조각** | `최악 INP(추정)`                                     |

그래서 "얼마나 빨라졌나"(총량)만 재면 트랜지션은 효과가 없어 보이고, 오히려 나빠 보이기까지 합니다.
거꾸로 최장 태스크만 보면 가상화가 **어느 단계**를 줄였는지 설명할 수 없습니다.
**두 축을 따로 재야 두 기법이 각각 평가됩니다** — 지표를 그룹으로 나눈 이유입니다(3절).

### 현재 결론 요약

1,000건 기준입니다 (4x 감속, 5회 중앙값 — [8. 참고 수치](#8-참고-수치)).

- **가상화는 총량을 줄입니다** — 결과 표시 4,873ms → 274ms (18배), 총 작업량 4,934ms → 302ms. 등급은 나쁨 → 개선 필요.
- **트랜지션은 가장 긴 한 조각을 줄입니다** — 최장 태스크 233ms → 95ms (2.5배), 추정 INP 249ms → 111ms. 등급은 개선 필요 → **좋음**.
  총 작업량은 302 → 324ms로 오히려 늘고 결과 표시도 274 → 292ms로 늦어집니다 — 0절 표가 말하는 쪼개기의 대가가 그대로 나타납니다.
- **둘은 대체재가 아닙니다.** 가상화만으로는 "좋음"에 닿지 못하고(최장 태스크 233ms), 트랜지션을 얹어야 닿습니다.

---

## 이 측정이 답하는 질문

> **조회 결과를 그리는 동안, 사용자 입력이 최대 얼마나 막혔나.**

INP는 입력이 있어야만 생기는 값이라 직접 잴 수 없습니다. 아무도 누르지 않으면 화면이 1.2초 멈춰 있어도 INP는 0입니다.
그래서 "가장 운 나쁜 사용자"를 가정한 상한을 씁니다.

```
최악 INP(추정) = 측정 구간 내 가장 긴 태스크 + 16ms(페인트 한 번)
```

**프레임이 아니라 태스크**에서 출발하는 이유: 브라우저는 실행 중인 태스크가 끝나야 입력을 배달합니다.
프레임이 1,000ms여도 그 안이 5ms 태스크 200개로 쪼개져 있으면 입력은 5ms만 기다립니다.
프레임 전체를 하나의 차단으로 보면, 작업을 잘게 쪼개는 구현(트랜지션)이 부당하게 나쁘게 나옵니다.

태스크가 끝나면 입력 핸들러가 돌고(가볍다고 전제) 그 결과가 그려지는 페인트 한 번을 더 기다리므로 16ms를 더합니다.

| 항목      | 내용                                                                           |
| --------- | ------------------------------------------------------------------------------ |
| 측정 대상 | 조회 클릭 → 결과가 화면에 그려질 때까지의 메인 스레드 태스크                   |
| 비교 대상 | 테이블 렌더 · 가상화 · 가상화+트랜지션 × 200/1,000건 (6개 시나리오)            |
| 핵심 지표 | 트랜지션은 `최악 INP(추정)`, 가상화는 `결과 표시 시간` + 작업 분해 (3절)       |
| 실행      | `npm run build && npm run start` → `CPU_THROTTLE=4 npm run measure:order-grid` |
| 전제      | **프로덕션 빌드만** (개발 모드는 검증 코드·이중 렌더로 비용이 부풀려짐)        |
| 〃        | **실제 창(headed) + 화면 가득**. 헤드리스는 합성 비용이 실제와 다르다 (7절)    |

---

## 1. 측정 구간

```
조회 클릭 ─────── (API 600ms, 메인 스레드 유휴) ─────── 응답 ─── 렌더 ─── 페인트
└ 구간 시작                                                              구간 끝 ┘
```

### 끝을 "조회 완료"가 아니라 "페인트"로 잡는 이유

이게 이 측정의 핵심 설계입니다. 트랜지션(`useDeferredValue`)을 쓰면 **결과 렌더가 조회 완료 뒤에 옵니다.**
`isFetching`이 false가 되는 시점에 구간을 닫으면, 정작 재려던 렌더 비용이 구간 밖으로 빠져
**트랜지션을 켠 구현이 실제보다 빠르게 측정됩니다.**

그래서 구간의 끝은 `renderedResult`가 실제로 화면에 그려진 뒤입니다
(`lib/performance/next-paint.ts`의 `runAfterNextPaint` — rAF 두 번).

> 실측 확인: 이 설계로 바꾸자 `virtualized-transition@1000`에서 **이전에는 하나도 잡히지 않던 LoAF 프레임(60ms)이 잡혔습니다.**
> 구간 밖으로 빠지고 있었다는 직접적인 증거입니다.

### 시작을 "응답 도착"이 아니라 "조회 클릭"으로 잡은 이유

엄밀하게는 응답이 도착한 시점부터 재야 합니다. 그 앞은 렌더 비용이 아니니까요.
하지만 응답 도착 시각을 브라우저 안에서 렌더 **전에** 표시할 방법이 없습니다 — React에서 그 시점을 알 수 있는 건
이미 렌더가 끝난 뒤(effect)이고, 그때 마크를 남기면 정작 재려던 태스크가 구간 밖으로 나갑니다.

대신 **이 차이는 순위에 영향을 주지 않습니다.** 구간이 클릭부터 열려도 응답 전까지 도는 태스크는
클릭 핸들러 하나(수 ms)뿐이고 API 지연 600ms 동안 메인 스레드는 놀고 있습니다.
그 클릭 태스크가 최댓값이 되는 경우는 렌더가 클릭 처리보다도 빠른 경우뿐이며, 그때 판정은 어느 쪽이든 "좋음"입니다.

`결과 표시 시간`(renderCompleteMs)은 이 한계가 없습니다 — Resource Timing의 `responseEnd`부터 재므로
네트워크 시간이 빠진 순수 렌더 시간입니다.

---

## 2. 왜 CDP 트레이스를 쓰는가

**앱 안의 Long Tasks·LoAF만으로는 트랜지션 효과에 숫자를 붙일 수 없습니다.** 둘 다 관찰 하한이 50ms입니다.

트랜지션은 React가 재조정을 약 5ms 단위로 쪼개 브라우저에 양보하게 만듭니다.
그러면 **쪼개진 태스크는 하나도 기록되지 않아** "50ms 미만"이라는 사실만 남습니다.
"1,053ms → 50ms 미만"은 상한으로는 맞지만 "얼마나 줄었나"의 근거로는 빈칸에 가깝습니다.

CDP 트레이스의 `RunTask`는 하한이 없습니다. 실측으로 확인했습니다.

| 방법           | 8ms 태스크 15개를 재면        |
| -------------- | ----------------------------- |
| Long Tasks API | 아무것도 기록되지 않음        |
| CDP `RunTask`  | 8.7 / 8.1 / 8.1 … 개별로 잡힘 |

첫 측정에서 `virtualized-transition@1000`의 최장 태스크는 **44.9ms**로 나왔습니다 — 하한 아래라서
이전 방식으로는 보이지 않던 값입니다.

### 대가: 트레이스는 측정 대상을 8% 느리게 만든다

동일 작업을 트레이스 on/off로 3회씩 비교한 결과입니다.

```
tracing=off  layout 177.3 / 173.7 / 179.5 ms
tracing=on   layout 190.2 / 191.5 / 193.9 ms   ← 약 8% 느림
```

모든 시나리오에 똑같이 걸리므로 **시나리오 간 비율 비교에는 무해**합니다.
다만 이 문서의 절대값을 트레이스 없이 잰 다른 측정과 비교하면 안 됩니다.

### 카테고리 수는 커밋 값을 바꾸지 않는다

카테고리를 늘리면 `Commit`이 1.6ms → 42~47ms로 부푼다는 기록이 있었지만 **재현되지 않았습니다.**
트레이스 조건만 바꿔 **번갈아 5회씩** 잰 결과입니다 (2026-09-14, 4x, headed · 화면 가득, 회차별 최대값의 범위).

| 트레이스 카테고리         | `Commit` 최대 `virtualized@1000` | `Commit` 최대 `virtualized-transition@1000` | 결과 표시 (transition, 중앙값) |
| ------------------------- | -------------------------------- | ------------------------------------------- | ------------------------------ |
| 끔                        | —                                | —                                           | 279ms                          |
| **현재 3개**              | 1.2~2.1ms                        | 1.5~2.2ms                                   | 282ms                          |
| 3개 + `loading`           | 1.3~2.0ms                        | 1.1~2.3ms                                   | 288ms                          |
| DevTools 기본 세트 (18개) | 1.4~1.9ms                        | 1.3~2.4ms                                   | 303ms                          |

DevTools 기본 세트를 전부 켜도 커밋은 2ms 안팎이고, 늘어나는 것은 트레이스 자체의 오버헤드(결과 표시 +8%)뿐입니다.
트레이스를 끈 회차의 결과 표시가 3개일 때보다 짧으므로, **이 측정이 커밋 비용을 숨기고 있지도 않습니다.**

`Commit`은 메인 스레드가 컴포지터 스레드의 커밋 완료를 **기다릴 수 있는** 구간입니다(`ProxyMain::BeginMainFrame`).
그래서 컴포지터·GPU 쪽이 바쁘면 길어집니다. 이전 기록은 카테고리만 바꿔가며 연속으로 잰 값이었고,
규모가 7절의 "측정이 겹치면 커밋 2 → 68ms"와 비슷해 **측정 중 다른 부하**가 가장 유력하지만 확인하지는 못했습니다.

> DevTools로 손수 잰 `Commit` 42ms와의 차이는 **여전히 풀리지 않았습니다**(9절 0).
> 카테고리 수·Screenshots·advanced paint instrumentation·GPU 가속 상태는 원인이 아님을 확인했습니다.

### 구간을 트레이스에서 어떻게 찾는가

앱이 구간을 열고 닫을 때 `performance.mark()`를 남깁니다
(`MEASUREMENT_WINDOW_MARK` — `lib/performance/responsiveness-tracker.ts`).
마크는 트레이스에 `blink.user_timing` 이벤트로 들어가므로, **트레이스 시간축과 `performance.now()`를
맞추는 계산이 필요 없습니다** — 마크 자체가 경계입니다.

마크가 있던 pid/tid가 측정 대상의 메인 스레드라는 것도 함께 알 수 있습니다
(트레이스에는 컴포지터·워커·다른 탭이 섞여 있어 이 식별이 필요합니다).

덤으로 DevTools Performance 패널의 Timings 트랙에도 그대로 보여 수동 분석에 쓸 수 있습니다.

트레이스 카테고리는 세 개입니다.

| 카테고리                                | 쓰는 이벤트                                                                |
| --------------------------------------- | -------------------------------------------------------------------------- |
| `disabled-by-default-devtools.timeline` | `RunTask` — 태스크 경계. **하한 없음**                                     |
| `devtools.timeline`                     | 작업 종류별 이벤트 — `FunctionCall`·`UpdateLayoutTree`·`Layout`·`Paint` 등 |
| `blink.user_timing`                     | 구간 마크                                                                  |

> `RunTask`가 `devtools.timeline`이 아니라 `disabled-by-default-devtools.timeline`에 있습니다.
> 카테고리를 잘못 넣으면 에러 없이 **값이 조용히 null**이 되므로, 테스트가 `longestTaskMs`가 null이면 실패시킵니다.

### 자르는 단위는 마크가 아니라 태스크다

마크 시각으로 그대로 자르면 **태스크가 반으로 잘립니다.** 종료 마크는 rAF 콜백에서 찍히는데,
브라우저는 **같은 태스크 안에서** 그 뒤에 스타일 계산·레이아웃·페인트·커밋을 이어서 하기 때문입니다.

```
RunTask 47.4ms ┌ PrePaint 3.0
               │ ← 종료 마크
               │ Paint 3.7 · Layerize 1.7 · Commit 35.7   ← 마크 뒤
               └
```

이때 잘라내면 자식(Paint·Commit)만 빠지고 부모 `RunTask`는 남습니다. 셀프 타임은 "길이 − 직계 자식"이라
**빠진 자식의 시간이 부모에게 남고, 부모는 `기타(System)`로 분류**됩니다. 비용이 사라지는 게 아니라
**잘못된 칸으로 옮겨갑니다** — 총 작업량과 분해 합이 여전히 일치해서 눈에 띄지도 않습니다.
실측에서 커밋 35.7ms가 통째로 기타로 갔고, 그래서 페인트가 실제보다 작게 보였습니다.

태스크는 원자 단위입니다. **구간에 걸쳤으면 통째로 넣고, 아니면 통째로 뺍니다**
(`snapToTaskBoundaries` — `e2e/order-grid/main-thread-trace.ts`).
경계를 넓혀도 새 태스크가 딸려 들어오지는 않습니다 — 넓힌 구간은 그 경계 태스크가 차지하고 있고
태스크끼리는 겹치지 않기 때문입니다.

---

## 3. 지표

**지표는 무엇을 평가하느냐로 나뉩니다.** 두 기법이 줄이는 것이 다르기 때문입니다(0절).
한 지표로 둘을 평가하면 한쪽이 부당하게 평가됩니다 — 총량만 보면 트랜지션은 효과가 없거나 나빠 보이고,
최장 태스크만 보면 가상화가 어느 단계를 줄였는지 설명할 수 없습니다.

정의는 `e2e/order-grid/responsiveness-metrics.ts`의 `METRICS`에 있고, 리포터도 이 그룹 순서로 출력합니다.

### 입력 차단 — 트랜지션 평가

| 지표                                 | 뜻                          | 출처          |
| ------------------------------------ | --------------------------- | ------------- |
| `worstCaseInp` **최악 INP(추정)**    | 최장 태스크 + 16ms          | 계산          |
| `longestTaskMs` └ 최장 태스크        | 입력이 실제로 기다리는 단위 | CDP `RunTask` |
| `blockingTaskCount` └ 차단 태스크 수 | 16ms 이상 태스크 개수       | CDP `RunTask` |

전체 태스크 수가 아니라 16ms 이상만 세는 이유는 CDP에 하한이 없다는 것의 이면입니다 —
실측에서 구간 내 84개 태스크 중 **57개가 0.1ms 미만(합 0.6ms)**이었습니다. 전부 세면 이 잡음에 묻힙니다.

> **차단 태스크 수는 쪼갰다고 줄지 않습니다.** 200ms 태스크 하나를 90 · 50 · 18ms로 쪼개면 개수는 1 → 3으로
> **늘어납니다**. 실제로 1,000건에서 가상화 2개 → 가상화+트랜지션 3개입니다(8절).
> 쪼개기가 듣는지는 **최장 태스크**로 판단하고, 이 값은 "덩어리가 몇 개로 나뉘었나"를 보는 보조 지표로 읽습니다.

### 작업량 — 가상화 평가

| 지표                                  | 뜻                                                               | 출처                               |
| ------------------------------------- | ---------------------------------------------------------------- | ---------------------------------- |
| `renderCompleteMs` **결과 표시 시간** | 응답 완료 → 첫 행 페인트. 사용자가 체감하는 값                   | Resource Timing + MutationObserver |
| `totalTaskMs` 총 작업량               | 아래 넷의 합. **쪼개기는 이 값을 줄이지 않는다**                 | CDP `RunTask`                      |
| `scriptingMs` └ 스크립트              | React 재조정·핸들러·파싱·GC                                      | CDP 트레이스                       |
| `renderingMs` └ 렌더링                | 스타일 계산·레이아웃                                             | 〃                                 |
| `paintingMs` └ 페인트                 | `Paint` + **`Commit`**(컴포지터 커밋 대기). **메인 스레드 몫만** | 〃                                 |
| `systemMs` └ 기타                     | 태스크 오버헤드·미분류                                           | 〃                                 |

**`결과 표시 시간`이 가상화 개선의 대표 지표**이고, 나머지 넷이 "그 시간을 어디서 줄였는지"를 설명합니다.
동시에 이 지표는 **트랜지션의 대가**도 드러냅니다 — 입력은 덜 막지만 결과는 늦게 뜰 수 있습니다(8절).

### 교차 검증

| 지표                               | 뜻                             | 출처 |
| ---------------------------------- | ------------------------------ | ---- |
| `longestFrameDuration` 최장 프레임 | 50ms 하한이 있어 빈칸이 생긴다 | LoAF |
| `longFrameCount` 긴 프레임 수      | 〃                             | LoAF |

프레임은 **대표 지표로 쓰지 않습니다.** `plain`은 렌더 전체가 한 태스크라 최장 프레임이 최장 태스크와
사실상 같은 값이고(중복), 가벼운 시나리오에서는 하한 때문에 아예 빈칸이 됩니다.
프레임은 "얼마나 멈췄나"를 보는 단위지 "얼마나 일했나"를 보는 단위가 아닙니다.

### 작업 분류는 DevTools Summary와 같은 기준이다

`scripting`/`rendering`/`painting`/`system`은 DevTools Performance 패널의 Summary와 같은 분류입니다.
**CDP가 이 요약을 직접 주지는 않습니다** — DevTools가 트레이스 이벤트를 분류해 계산하는 값이라,
`main-thread-trace.ts`에서 같은 방식으로 계산합니다. 그래서 DevTools로 잰 다른 앱 수치와 바로 비교할 수 있습니다.

세 가지를 주의합니다.

1. **셀프 타임으로 합산합니다.** 이벤트는 중첩돼 있어서(`RunTask ⊃ FunctionCall ⊃ Layout`) 길이를 그대로
   더하면 같은 시간을 두 번 셉니다. 각 이벤트에서 직계 자식의 길이를 뺀 값을 씁니다 — DevTools Summary도 같은 기준입니다.
2. **분류표는 근사치입니다.** 이름이 목록에 없는 이벤트는 `system`으로 보내 스크립트·렌더링 값을 부풀리지 않습니다.
   Chromium 버전을 올리면 한 번은 DevTools Summary와 나란히 놓고 맞춰봐야 합니다.
3. **페인트는 메인 스레드 몫만 잡힙니다.** 래스터·GPU는 다른 스레드입니다
   (실측: 같은 구간의 `RasterTask` 2.8ms · `GPUTask` 12.4ms는 집계에 없습니다).
   DevTools에서 Main 트랙을 선택했을 때의 Painting과 같은 기준입니다.

> **DevTools Summary와 나란히 볼 때 주의**: 손으로 잡은 선택 범위는 **결과가 그려진 뒤의 프레임까지** 포함하기 쉽습니다.
> 이 측정의 구간은 결과 페인트에서 닫히므로 그 뒤 프레임이 빠집니다. 실측 대조에서 렌더링만 52 vs 53ms로 일치하고
> (스타일·레이아웃은 결과 렌더에서 끝나므로) 스크립트·페인트만 차이가 났다면, 범위가 넓게 잡힌 것입니다.

`Performance.getMetrics`를 쓰지 않는 이유도 여기 있습니다 — **페인트 항목이 없고** 측정 구간을 잘라낼 수도 없습니다.
다만 교차 검증에는 유용해서, `ScriptDuration` 델타와 대조해보니 `table@1000`에서 3,805 vs 3,789ms로 일치했습니다.

### 집계에 표본 수(n)를 항상 붙인다

값이 없는 회차는 집계에서 빠지므로, 5회를 돌려도 1회만 반영된 중앙값이 나올 수 있습니다.
표본 수를 숨기면 그 한 회차가 대표값처럼 보이므로 `59 (n=1/5)` 형태로 함께 출력합니다.
표준편차는 **표본표준편차(÷n−1)**입니다 — 모분산(÷n)은 편차를 작게 내 차이를 과신하게 만듭니다.

---

## 4. 시나리오

**3개 그리드 × 2개 건수 = 6개.** 회차마다 시나리오를 번갈아 실행해 기기 상태 변화(발열 등)가 한 시나리오에 몰리지 않게 합니다.

| id                       | 구현                           | 경로                                          |
| ------------------------ | ------------------------------ | --------------------------------------------- |
| `plain`                  | 행을 전부 DOM에 그림           | `/examples/order-grid/table`                  |
| `virtualized`            | TanStack Virtual로 보이는 행만 | `/examples/order-grid/virtualized`            |
| `virtualized-transition` | 가상화 + `useDeferredValue`    | `/examples/order-grid/virtualized-transition` |

트랜지션 적용은 **테이블이 아니라 셸(`OrderGridShell`)의 `resultPriority` prop**입니다.
테이블의 성질이 아니라 시나리오의 축이고, 셸이 미루기를 직접 해야 "결과가 언제 화면에 있는지"(구간의 끝)를 알 수 있기 때문입니다.
덕분에 어떤 테이블 구현에든 트랜지션을 켜고 끌 수 있습니다.

### 시나리오가 하는 일

```
페이지 열기 → 하이드레이션 대기 → 네트워크 유휴 대기
  → 건수 라디오 선택 → 메인 스레드 유휴 대기 → 트레이스 시작 → 조회 클릭
  → 첫 행 페인트 대기 → 구간 종료(idle) 대기 → LoAF 값 안정 대기 → 트레이스 종료
```

**렌더 도중에 입력을 넣지 않습니다.** 처리를 기다리는 입력이 있으면 브라우저가 스타일·레이아웃을
`renderStart` 앞으로 당겨 실행해서 LoAF의 구간 분해가 망가집니다.

실측 (1,000행, 각 3회 — **이전 설정(헤드리스·1,440×900)에서 잰 값이라 절대값은 8절과 다릅니다**):

| 조건        | 렌더링 전 작업 | 스타일·레이아웃·페인트 준비 | CDP 실측(스타일+레이아웃) |
| ----------- | -------------- | --------------------------- | ------------------------- |
| 입력 없음   | 895~984ms      | **239~249ms**               | 208~215ms ✅ 일치         |
| 마우스 클릭 | 1,187~1,229ms  | **7~8ms**                   | 211~213ms ❌ 불일치       |
| 키 입력     | 1,123~1,186ms  | **1ms**                     | 202~209ms ❌ 불일치       |

일이 사라지는 게 아니라 칸만 옮겨가며, 프레임 총 길이는 거의 그대로입니다.
입력이 얼마나 막히는지는 `최악 INP(추정)`으로 갈음합니다.

> **이 제약은 이제 풀 수 있습니다.** 위 표의 오른쪽 열이 보여주듯 **CDP 실측은 입력이 있어도 흔들리지 않습니다**
> (208~~215 / 211~~213 / 202~209). 망가지는 것은 LoAF의 구간 분해뿐인데, 핵심 지표를 CDP로 옮긴 지금
> LoAF는 교차 검증용으로만 남았습니다.
> 실제로 입력을 주입해 재본 결과가 8절에 있습니다 — 태스크 측정은 멀쩡했고 실측 INP까지 함께 나왔습니다.
> 실측 INP를 정규 지표로 되살리는 것이 다음 과제입니다(9절 0-1).

---

## 5. 구조

### 앱 안 — 측정하는 쪽

```
lib/performance/
  next-paint.ts              페인트 뒤에 콜백 실행 (rAF 두 번) — 구간의 끝을 잡는다
  frame-blocking.ts          LoAF 관찰 → 프레임 길이·구간 분해·범인 스크립트
  long-task.ts               Long Tasks 관찰
  interaction-timing.ts      Event Timing 관찰 (분석 도구용)
  responsiveness-tracker.ts  구간과 겹치는 기록만 모아 요약 + 구간 마크
  responsiveness-probe.ts    요약을 자동화 측정으로 내보내는 통로
  inp-rating.ts              등급 기준 + 추정식 두 개(태스크 기반 / 프레임 기반)

hooks/use-query-responsiveness.ts  구간을 열고 닫으며 측정, 요약은 probe로 전송
lib/analytics.ts                   분석 도구 연결 지점 (현재 dev 콘솔 출력만)
```

측정 훅은 `order-grid-shell.tsx`에서 **한 번** 부릅니다. 세 그리드가 이 셸을 공유하므로 한 군데만 붙여도
세 페이지가 같은 조건으로 측정되며, 구분은 `measurementScope`로 합니다.

측정값은 **화면에 그리지 않습니다.** 훅은 React 상태 없이 트래커를 직접 구독해 보고만 하므로,
기록이 도착해도 리렌더가 일어나지 않고 측정이 렌더 비용을 부풀리지 않습니다.

추정식이 두 개인 이유: 실사용자 브라우저에는 CDP가 없습니다.
`estimateWorstCaseInpFromTask`는 하한 없는 태스크를 쓸 수 있을 때(자동화 측정), `estimateWorstCaseInp`는
프레임 구간에서 추정할 수밖에 없을 때(실사용자 분석) 씁니다.

### 관찰자는 계속 켜두고, 구간은 시각으로 판정한다

가장 무거운 프레임은 결과를 그리는 프레임인데, 기록은 그 프레임이 끝난 뒤 비동기로 도착합니다.
구간이 닫힐 때 관찰을 끊으면 그 기록을 받지 못하므로, 관찰자는 마운트 내내 유지하고 구간 판정은 시각으로 합니다.
구간이 닫힌 뒤 도착한 기록도 시각이 겹치면 반영하고, 구간 시작 전에 시작해 걸쳐 있는 기록도 포함합니다
(`overlapsWindow`). CDP 트레이스 쪽도 같은 규칙입니다.

### e2e — 조작·수집하는 쪽

```
playwright.config.ts                  워커 1개, 순차 실행, 트레이스·비디오 off
e2e/order-grid/
  responsiveness.spec.ts              시나리오 × 회차만큼 테스트 생성, CDP 세션 생성
  order-grid-page.ts                  페이지 조작 — 조회, 대기, 보고 수신(exposeFunction)
  main-thread-trace.ts                CDP 트레이스 수집 + 구간 잘라내기 (하한 없는 최장 태스크)
  responsiveness-metrics.ts           지표 정의, 통계 계산
  responsiveness-summary-reporter.ts  결과 표 출력 + JSON 저장
```

### 연결 지점 — `page.exposeFunction`

Playwright가 페이지 이동 전에 `window.__onResponsivenessReport`를 주입하고, 앱은 구간 상태나 요약이
갱신될 때마다 이 함수를 부릅니다. 실사용자 브라우저에는 함수가 없으므로 아무 일도 일어나지 않습니다.

```ts
type ResponsivenessProbeReport =
  | { status: "unsupported" }
  | { status: "measuring"; summary: ResponsivenessSummary | null }
  | { status: "idle"; summary: ResponsivenessSummary | null };
```

`status`는 **조회 상태가 아니라 구간 상태**입니다. 트랜지션에서는 조회가 끝난 뒤에도 구간이 열려 있으므로,
이 구분이 없으면 테스트가 측정을 일찍 끝냅니다.

---

## 6. 실행

```bash
npm run build && npm run start                 # 프로덕션 서버 (필수)
CPU_THROTTLE=4 npm run measure:order-grid      # 8절 기준 수치와 같은 조건 (6개 시나리오 × 5회)

npm run measure:order-grid                                # 감속 없이 (CPU_THROTTLE 기본값은 1)
npm run measure:order-grid -- --grep "virtualized@1000"   # 일부만
npm run measure:order-grid -- --ui                        # 단계별 실행
```

> 측정은 **실제 창을 띄워** 돕니다(headed). 창이 화면을 가리므로 도는 동안 다른 작업을 하지 마세요 — 부하가 값에 그대로 섞입니다(7절).

| 환경 변수        | 기본값                  | 뜻                       |
| ---------------- | ----------------------- | ------------------------ |
| `MEASURE_RUNS`   | 5                       | 시나리오별 반복 횟수     |
| `MEASURE_WARMUP` | 0                       | 집계에서 뺄 예열 회차 수 |
| `CPU_THROTTLE`   | 1                       | CPU 감속 배율            |
| `BASE_URL`       | `http://localhost:3000` | 측정할 서버 주소         |

> **주의:** 개발 서버가 3000번을 쓰고 있으면 `npm run start`가 조용히 실패하고 그쪽을 측정하게 됩니다.
> 측정이 개발 서버를 감지하면 실패하지만, 애초에 부딪히지 않으려면 다른 포트로 띄우고 `BASE_URL`을 넘기세요.
>
> ```bash
> npx next start -p 3100
> BASE_URL=http://localhost:3100 npm run measure:order-grid
> ```

결과는 콘솔 표와 `perf-results/order-grid-responsiveness.json`으로 남습니다.

---

## 7. 측정 위생

| 장치                                 | 위치                     | 목적                                                                                                                                                                                                         |
| ------------------------------------ | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `workers: 1`, `fullyParallel: false` | `playwright.config.ts`   | 측정끼리 CPU를 나눠 쓰지 않게                                                                                                                                                                                |
| `trace: 'off'`, `video: 'off'`       | 〃                       | Playwright 녹화가 부하를 더하지 않게 (CDP 트레이스와 별개)                                                                                                                                                   |
| **`headless: false`**                | 〃                       | 헤드리스는 합성 경로가 달라 커밋이 사실상 공짜가 된다 (같은 작업의 `Commit` 최대값: 헤드리스 0.3ms / 헤디드 1.7ms, 페인트 열 전체가 어긋난다)                                                                |
| **`viewport: null` + 창 최대화**     | 〃                       | 가상화가 그리는 행 수는 화면 높이에 달렸다. 기준 수치는 2,560×1,440 화면의 **2,480×1,328 뷰포트 · 36행**이며, 다른 화면에서 재면 절대값이 달라진다                                                           |
| **측정 중 다른 부하 금지**           | (수동)                   | 다른 측정이나 무거운 브라우저 작업이 동시에 돌면 값 전체가 밀린다. 실측에서 측정 두 개가 겹쳤을 때 최장 태스크 244 → 388ms, 커밋 2 → 68ms로 부풀었다                                                         |
| 테스트마다 새 브라우저 컨텍스트      | `responsiveness.spec.ts` | React Query 캐시가 회차 간 섞이지 않게                                                                                                                                                                       |
| 하이드레이션 대기                    | `waitForHydration`       | 하이드레이션 전 클릭은 폼 제출(새로고침)이 됨                                                                                                                                                                |
| 네트워크 유휴 대기                   | `goto`                   | 링크 prefetch가 측정 구간에 섞이지 않게                                                                                                                                                                      |
| 메인 스레드 유휴 대기                | `waitForMainThreadQuiet` | 최근 500ms간 긴 프레임이 없고 `requestIdleCallback`이 돌 때까지                                                                                                                                              |
| 구간 종료 대기                       | `waitForSettledSummary`  | `status: 'idle'` = 결과가 그려져 구간이 닫힘                                                                                                                                                                 |
| LoAF 값 안정 대기                    | 〃                       | LoAF는 비동기로 도착하므로 800ms간 값이 그대로면 종료                                                                                                                                                        |
| 개발 서버 차단                       | `assertProductionBuild`  | 포트 충돌로 개발 서버를 조용히 재는 것을 실패시킨다                                                                                                                                                          |
| 태스크 지표 null 차단                | `responsiveness.spec.ts` | 트레이스에서 구간을 못 찾으면 값 없이 통과하지 않게                                                                                                                                                          |
| **재빌드 후 서버 재시작**            | (수동)                   | 서버를 띄운 채 `npm run build`를 하면 서버의 빌드 ID와 디스크 청크가 어긋나 JS가 404 난다. 증상은 **하이드레이션 대기 타임아웃**이라 원인이 드러나지 않는다 — 측정 코드를 의심하기 전에 서버부터 다시 띄운다 |
| 시나리오 교차 실행                   | 〃                       | 발열 등 시간에 따른 변화가 한 시나리오에 몰리지 않게                                                                                                                                                         |

---

## 8. 참고 수치

2026-09-14, Apple Silicon, 프로덕션 빌드, **headed · 2,480×1,328 뷰포트**, **`CPU_THROTTLE=4`**,
`MEASURE_RUNS=5` 중앙값. **기기·화면 크기·부하에 따라 크게 달라지므로 절대값이 아니라 시나리오 간 비율로 읽으세요.**

**입력 차단 — 트랜지션 평가**

| 시나리오                      | 등급          | 추정 INP  | 최장 태스크 | 차단 태스크 |
| ----------------------------- | ------------- | --------- | ----------- | ----------- |
| `plain@200`                   | **나쁨**      | 837       | 821         | 4           |
| `plain@1000`                  | **나쁨**      | **3,742** | 3,726       | 5           |
| `virtualized@200`             | **개선 필요** | 249       | 233         | 3           |
| `virtualized@1000`            | **개선 필요** | **249**   | 233         | 2           |
| `virtualized-transition@200`  | 좋음          | 115       | 99          | 4           |
| `virtualized-transition@1000` | 좋음          | **111**   | **95**      | 3           |

**작업량 — 가상화 평가** (분해 넷의 합 = 총 작업량)

| 시나리오                      | 결과 표시 | 총 작업량 | 스크립트 | 렌더링 | 페인트 | 기타 |
| ----------------------------- | --------- | --------- | -------- | ------ | ------ | ---- |
| `plain@200`                   | 1,096     | 1,169     | 845      | 215    | 43     | 74   |
| `plain@1000`                  | **4,873** | **4,934** | 3,757    | 997    | 60     | 114  |
| `virtualized@200`             | 271       | 334       | 219      | 53     | 10     | 51   |
| `virtualized@1000`            | **274**   | **302**   | 217      | 53     | 9      | 22   |
| `virtualized-transition@200`  | 288       | 356       | 237      | 55     | 11     | 51   |
| `virtualized-transition@1000` | 292       | 324       | 232      | 56     | 13     | 26   |

최장 태스크 회차별 값 (5회) — 가상화와 트랜지션은 **범위가 겹치지 않습니다**:

```
plain@1000                   3,736 / 3,671 / 3,748 / 3,726 / 3,687
virtualized@1000               235 /   233 /   230 /   229 /   233
virtualized-transition@1000     95 /    92 /    96 /    95 /    98   ← 겹치지 않는다
```

### 읽는 법

**가상화 — 총량을 줄입니다. 어디서 줄었는지가 분해로 보입니다.** 1,000건 기준:

```
결과 표시    4,873ms → 274ms   (18배)
총 작업량    4,934ms → 302ms   (16배)
├ 스크립트   3,757ms → 217ms   (17배)  ← React 재조정·DOM 생성. 비용의 76%를 차지하던 항목
├ 렌더링       997ms →  53ms   (19배)  ← 스타일 계산·레이아웃
└ 페인트        60ms →   9ms           ← 메인 스레드 몫만 (3절)
```

비용의 대부분이 **스크립트(76%)**였고, 가상화가 그릴 노드를 줄이자 스크립트와 렌더링이 함께 줄었습니다.

**다만 가상화만으로는 "좋음"에 닿지 못합니다.** 최장 태스크가 233ms로 남아 등급은 여전히 개선 필요입니다.
줄어든 것은 **총량**이지 한 조각의 길이가 아니기 때문입니다.

**트랜지션 — 남은 한 덩어리를 쪼갭니다.**

- 최장 태스크 233ms → **95ms (2.5배)**, 추정 INP 249 → 111ms. **개선 필요 → 좋음**으로 등급이 바뀝니다.
- 회차별 범위가 겹치지 않습니다 (229~~235 vs 92~~98). 잡음이 아닙니다.
- 트레이스에서 쪼개진 모습이 직접 보입니다 — 가상화의 **213ms 한 덩어리**가 **92 · 50 · 18ms**로 나뉩니다.
  그래서 `차단 태스크` 개수는 2 → 3으로 **늘어납니다**(3절 주석).
- 대가도 표에 있습니다: 총 작업량 302 → 324ms, 결과 표시 274 → 292ms. **입력은 덜 막지만 결과는 조금 늦게 뜹니다.**

거꾸로 `plain@1000`은 차단 태스크가 5개인데 최장이 3,726ms입니다 — 렌더 전체가 사실상 **한 태스크**라는 뜻입니다.
트랜지션 없이 가상화만 걸면 그 한 덩어리가 233ms로 작아질 뿐, 여전히 한 덩어리입니다.

### 다른 앱과 같은 그림이다

다른 앱(같은 성격의 주문 그리드)에서 DevTools로 실측한 값과 나란히 놓으면 방향이 같습니다.

|               | 그 앱 (DevTools 실측)                               | 이 앱 (1,000건, 4x 감속)                                    |
| ------------- | --------------------------------------------------- | ----------------------------------------------------------- |
| 가상화만      | Scripting 823ms · Total 1,343ms · **INP 1,228ms**   | 스크립트 217ms · 총 작업량 302ms · **최장 태스크 233ms**    |
| 트랜지션 추가 | Scripting 988ms · Total **2,040ms** · **INP 130ms** | 스크립트 232ms · 총 작업량 **324ms** · **최장 태스크 95ms** |

양쪽 다 **총 작업량은 늘었는데 입력 차단은 줄었습니다.** 쪼개기가 동작할 때의 전형적인 모습입니다(0절의 표).
규모는 그 앱이 훨씬 크지만(감속을 빼면 이 앱의 스크립트는 60ms 남짓), 비율로 보면 같은 현상입니다.

> 이전 판 문서는 여기서 "이 앱은 쪼갤 일이 없어 트랜지션 효과가 보이지 않는다"고 결론지었습니다.
> 그 결론은 **측정 쪽 문제 두 가지** 때문이었습니다 — 헤드리스라 합성 비용이 빠졌고(7절),
> 구간을 마크로 잘라 페인트·커밋이 기타로 새어나갔습니다(2절). 둘을 고치자 쪼갤 덩어리가 드러났습니다.

### 참고: 입력을 넣으면 더 쪼개질까 (이전 설정에서 확인, 결과 동일)

React 스케줄러는 `isInputPending()`으로 **대기 중인 입력이 있을 때만** 양보하고, 없으면 최대 300ms까지 한 조각으로 돕니다.
그렇다면 "렌더 도중 입력을 넣지 않는" 이 측정의 설계가 트랜지션에 불리한 것 아닌가 — 확인했습니다.

아래는 **이전 설정(헤드리스·1,440×900·구간 자르기 수정 전)**에서 잰 값이라 절대값은 위 표와 다릅니다.
당시에는 쪼갤 덩어리 자체가 드러나지 않아 차이가 보이지 않았습니다. 지금 조건에서 다시 재볼 가치가 있습니다(9절 0).

렌더가 도는 동안 CDP로 25ms마다 클릭을 40회 주입한 결과입니다 (4x 감속).

| 시나리오                 | 입력     | 최장 태스크 | 16ms 이상 | 태스크 합 | **실측 INP** |
| ------------------------ | -------- | ----------- | --------- | --------- | ------------ |
| `virtualized`            | 없음     | 147ms       | 4개       | 321ms     | 24ms         |
| `virtualized`            | **있음** | 115ms       | 3개       | 187ms     | **136ms**    |
| `virtualized-transition` | 없음     | 114ms       | 3개       | 267ms     | 16ms         |
| `virtualized-transition` | **있음** | 117ms       | 2개       | 199ms     | **136ms**    |

입력을 넣어도 최장 태스크는 거의 그대로였습니다. 입력 유무는 쪼개기의 조건이 아니라는 뜻입니다.

> 부수 소득: 입력을 주입해도 **CDP 태스크 측정은 멀쩡합니다.** 입력이 망가뜨리는 것은 LoAF의 구간 분해뿐입니다(4절).
> 즉 핵심 지표를 CDP로 옮긴 지금은 **실측 INP를 함께 잴 수 있습니다** — 위 표가 그 결과입니다.

### 화면 크기는 행 수를 조금 바꾼다

가상화는 보이는 행만 그리므로 화면이 커지면 비용이 늘 수 있습니다. 확인했습니다 (4x 감속, 1,000건).

| 뷰포트                | 스크롤 컨테이너 | 그려진 데이터 행 | `td`         | 최장 태스크   |
| --------------------- | --------------- | ---------------- | ------------ | ------------- |
| 1440×900              | 628px           | 28개             | 561개        | 166ms         |
| 1920×1080             | 754px           | 31개             | 621개        | 163ms         |
| 2480×1328 (현재 설정) | 1006px          | 36개             | 701개        | 233ms         |
| (비교) `plain`        | —               | **1,000개**      | **20,000개** | 3,700~4,800ms |

행 수는 화면을 크게 키워도 28 → 36개로 조금 늘 뿐입니다(`max-h-[70vh]` + 행 높이 37px + overscan 10).
위 세 줄은 잰 시점의 설정이 달라(앞 두 줄은 헤드리스) 최장 태스크를 직접 비교할 수 없습니다.
**화면 크기를 바꾸면 기준 수치를 다시 재야 합니다** — 그래서 7절에 뷰포트를 명시해 두었습니다.

### 결과가 그려진 뒤에도 프레임이 하나 더 온다

구간이 닫힌 뒤 100~300ms 지점에 스크립트가 전혀 없는 렌더 프레임이 한 번 더 옵니다
(실측: `UpdateLayoutTree 21.5 → Layout → PrePaint → Paint 4.3 → Commit`).

범인은 셸의 `transition-opacity`입니다 — 조회 중 `opacity-50`이던 것이 결과와 함께 100%로 돌아가고,
**그 CSS 트랜지션이 끝날 때** 스타일이 다시 계산됩니다. 페이지에 `transition: none`을 주입해 확인했습니다.

| 구간 종료 후 600ms | 트랜지션 유지 | 트랜지션 제거 |
| ------------------ | ------------- | ------------- |
| Paint              | 9.2ms         | 0             |
| Layout·PrePaint    | 0.5 · 1.5ms   | 0 · 0.0ms     |
| Commit             | 0.9ms         | 0.1ms         |

이 비용은 **구간 밖이라 지표에 들어오지 않습니다.** 재려던 것이 "결과를 그리는 비용"이므로 의도한 동작이지만,
DevTools로 손수 범위를 잡아 비교할 때는 이 프레임이 포함되기 쉬워 값이 커 보입니다(3절 주석).

---

## 9. 현재 알려진 한계

| #   | 한계                                           | 영향                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | **수동 DevTools 측정과 커밋 값이 맞지 않는다** | 손으로 잰 DevTools는 `Commit` 42ms, 자동 측정은 2ms 안팎이다. 브라우저 종류·녹화 옵션·GPU 가속 상태·**트레이스 카테고리 수**(2절)는 원인이 아님을 확인했다. 남은 의심은 **측정 중 다른 부하**(7절: 겹치면 커밋 2 → 68ms)와 브라우저 프로필 차이(확장 등)다. 풀릴 때까지 DevTools로 잰 커밋 값과는 비교하지 않는다 |
| 0-1 | **`plain+transition` 시나리오가 없다**         | 트랜지션 효과는 이제 `virtualized`에서 확인된다(233 → 95ms). 남은 질문은 **비용이 여러 파이버에 퍼진 `plain`에서는 얼마나 쪼개지는가**다. 셸의 `resultPriority` prop이므로 페이지 하나만 추가하면 된다                                        |
| 0-2 | **실측 INP를 정규 측정에서 재지 않는다**       | 지금은 렌더 도중 입력을 넣지 않아 실측 INP가 조회 버튼 클릭값(수십 ms)만 잡히므로 지표에서 제외했다. 핵심 지표를 CDP로 옮긴 뒤로는 **입력을 주입해도 태스크 측정이 멀쩡하다는 것을 확인했으므로**(8절), 실측 INP를 정규 지표로 되살릴 수 있다 |
| 0-3 | **측정값이 실행 기기의 화면 크기에 묶인다**    | `viewport: null` + 창 최대화라 화면이 다르면 그리는 행 수가 달라진다(8절). 기준 수치는 2,480×1,328 · 36행이다. 기기를 바꾸면 전 시나리오를 다시 재야 비교가 성립한다                                                                          |
| 1   | 구간 시작이 응답 도착이 아니라 조회 클릭       | 클릭 핸들러 태스크(수 ms)가 구간에 포함된다. 렌더가 그보다 빠른 경우에만 최댓값이 될 수 있고, 그때 판정은 어느 쪽이든 "좋음"이라 순위에 영향 없음 (1절 참고)                                                                                  |
| 2   | CDP 트레이스가 측정 대상을 약 8% 느리게 만든다 | 전 시나리오 공통이라 비율 비교에는 무해. 절대값을 트레이스 없는 측정과 비교하면 안 됨                                                                                                                                                         |
| 3   | `결과 표시 시간`의 이중 rAF                    | "페인트 직후"가 아니라 다음 프레임 시작이라 약 16ms 과대. 전 시나리오 공통                                                                                                                                                                    |
| 4   | LoAF 프레임 지표는 여전히 50ms 하한            | 교차 검증용으로만 둔 값이라 빈칸이어도 결론에 영향 없음. 표본 수(n)로 드러난다                                                                                                                                                                |
| 5   | `mergeInteractionEvent`가 web-vitals와 다름    | `min(startTime)`과 `max(duration)`을 따로 취해 서로 다른 이벤트의 값이 섞인다. **분석 도구로 보내는 `slow-interaction` 이벤트에만** 영향 (측정 지표에서는 제거됨)                                                                             |
| 6   | Chromium 전용                                  | LoAF(123+)·CDP 모두 Chromium에만 있다. Firefox·WebKit에서는 측정 자체가 불가                                                                                                                                                                  |
| 7   | Next.js/React 내부 구현 의존 2곳               | `assertProductionBuild`는 `hmr-client` 스크립트 이름, `waitForHydration`은 `__reactProps$` 키를 본다. 업그레이드에 조용히 깨질 수 있다                                                                                                        |
| 8   | 건수 목록이 3곳에 하드코딩                     | `options.ts`의 `ORDER_ROW_COUNT_OPTIONS`가 진짜 소스인데 spec의 `ROW_COUNTS`와 page object의 `RowCount` 타입이 따로 있다. 건수를 늘릴 때 타입 에러 없이 시나리오가 빠질 수 있다                                                               |
| 9   | 스크롤 반응성 미측정                           | 가상화의 비용은 초기 렌더보다 스크롤 중 재계산에 있다. 지금은 조회 한 번만 재서 가상화에 유리한 쪽만 본다                                                                                                                                     |
